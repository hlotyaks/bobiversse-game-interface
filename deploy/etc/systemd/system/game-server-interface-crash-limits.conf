[Unit]
# Stop retrying a failed game after five starts within five minutes. The
# controller shows CRASH LOOP and a manual retry resets this systemd limit.
StartLimitIntervalSec=300
StartLimitBurst=5
